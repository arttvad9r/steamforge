<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".SteamforgeApp"
        android:allowBackup="true"
        android:appCategory="game"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.Steamforge">

        <!--
          Yandex Mobile Ads 8 initializes automatically by default. Steamforge must apply the
          persisted privacy choice before initialization, so auto-init is explicitly disabled.
          AdsManager performs the manual initialization after analyticsConsent becomes non-null.
        -->
        <meta-data
            android:name="com.yandex.mobile.ads.AUTOMATIC_SDK_INITIALIZATION"
            android:value="false" />

        <!-- RuStore Pay reads these resources during release-process initialization. -->
        <meta-data
            android:name="console_app_id_value"
            android:value="@string/rustore_console_application_id" />
        <meta-data
            android:name="sdk_pay_scheme_value"
            android:value="@string/rustore_pay_scheme" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${rustorePayScheme}" />
            </intent-filter>
        </activity>
    </application>

</manifest>
